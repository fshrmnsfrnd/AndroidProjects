<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Intents">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DetailActivity"
            android:exported="false">
            <intent-filter>
                <action android:name="DetailActivity" />
            </intent-filter>
        </activity>
        <activity
            android:name=".WebBrowserActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="WebBrowserActivity" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ViewMapActivity"
            android:exported="false">
            <intent-filter>
                <action android:name="ViewMapActivity" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SnapshotActivity"
            android:exported="false">
            <intent-filter>
                <action android:name="SnapshotActivity" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CreateAlarmActivity"
            android:exported="false">
            <intent-filter>
                <action android:name="CreateAlarm" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DialerActivity"
            android:exported="false">
            <intent-filter>
                <action android:name="DialerActivity" />
            </intent-filter>
        </activity>
    </application>

</manifest>